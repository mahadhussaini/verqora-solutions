datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

model ContactLead {
  id        Int      @id @default(autoincrement())
  name      String
  email     String
  message   String
  submittedAt DateTime @default(now())
}
